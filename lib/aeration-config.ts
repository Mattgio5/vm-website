// Seasonal availability shown on /services/aeration-overseeding.
// UPDATE THIS NUMBER EACH MORNING based on approved quotes — it is the only
// place the count lives; every mention on the page reads from here.
export const AERATION_SPOTS_REMAINING = 175
